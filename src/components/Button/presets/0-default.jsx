import * as React from 'react';
import Button from '../Button';

/**
 * NOTE:
 * The style width prop is necessary so that it always
 * fills the space reserved for it in UXPin.
 */

export default <Button uxpId="button1" style={{ width: '100%' }} />;

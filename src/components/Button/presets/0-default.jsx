import React from 'react';
import Button from '../Button';

export default (
  <Button
    uxpId="primary-button-1"
    text="Basic Button"
    primary={true}
    iconName=""
    disabled={false}
    iconPosition="start"
    tooltip=""
    onClick={() => {}}
  />
);
